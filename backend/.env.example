# Server
PORT=5001

# MongoDB (legacy, still used until Phase 3 cutover)
MONGODB_URI=mongodb://localhost:27017/moodle-admin-instances

# PostgreSQL (Prisma)
DATABASE_URL="postgresql://postgres@127.0.0.1:5432/moodle_admin?schema=public"

# Auth
AUTH_TOKEN_SECRET=change-this-secret
